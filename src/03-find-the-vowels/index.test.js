import { describe, expect, test } from 'vitest'
import { findTheVowels } from './index.js'

describe('Find the Vowels', () => {
	test('should do something', () => {
		expect(findTheVowels()).toBe()
	})
})
