import { describe, expect, test } from 'vitest'
import { turkishNumbers } from './index.js'

describe('Turkish Numbers', () => {
	test('Should do something', () => {
		expect(turkishNumbers()).toBe()
	})
})
